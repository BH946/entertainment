# 테스트 코드는 메모리 DB를 사용하기 위해 전부 주석

spring:
  jpa:
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
#
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
#
logging:
  level:
    com.cafe24: debug
