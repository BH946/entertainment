# default 프로필 -> 개발모드로 사용
spring:
  datasource: #참고로 allowPublicKeyRetrieval=true: MySQL 8.0 이상에서 필요한 보안 설정
    url: jdbc:mysql://localhost:3306/entertainment_testdb?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

#    url: jdbc:h2:tcp://localhost/~/entertainment/tarot
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

  #    url: jdbc:oracle:thin:@localhost:1521:STR
  #    username: testUser
  #    password: 1234
  #    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
#      ddl-auto: create # table 자동 생성
      ddl-auto: none # db 초기화 없앰
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

server:
  servlet:
    session:
      timeout: 30m # session timeout 30min

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace

logging:
  level:
    com.cafe24: debug # 배포는 기본값(=info) 사용


--- #프로필 구분자
# prod 프로필 -> 배포모드로 사용
spring:
  config:
    activate:
      on-profile: prod
  # MYSQL DB
  datasource: # 참고로 allowPublicKeyRetrieval=true: MySQL 8.0 이상에서 필요한 보안 설정
    url: jdbc:mysql://localhost:3306/entertainment?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: etUser #그냥 간단히 하려고 권한은 다 줬음.
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none # DB 초기화 사용 X
#      ddl-auto: create # DB 초기화 사용
server:
  port: 8080 # 포트 포워딩으로 http(80) -> 8080 설정하면 "URL에 포트없이 바로 접속가능"
  servlet:
    session:
      timeout: 30m # session timeout 30min
logging:
  level:
    com.cafe24: info # 배포는 기본값(=info) 사용